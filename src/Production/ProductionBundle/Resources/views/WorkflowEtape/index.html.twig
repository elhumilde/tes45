{% extends '::base.html.twig' %}

{% block body -%}
    <h1>WorkflowEtape list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Idworkflow</th>
                <th>Idetapeprecedente</th>
                <th>Tempsprevu</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('workflowetape_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titre }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.idWorkflow }}</td>
                <td>{{ entity.idEtapePrecedente }}</td>
                <td>{{ entity.tempsPrevu }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('workflowetape_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('workflowetape_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('workflowetape_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
